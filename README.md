# @DepuracionMLB — V0.7 Reasoning Guard

Connected control plane for `@DepuracionMLB` / `@MLBdepuración`.

## Current authority

- Methodology: `DEP-MLB-V0.5`
- Mother Drive ID: `1nod0j35fZeILy7fxUk5IPCT7_qn3f0sEFsxmLbDdfrA`
- Mother SHA-256: `44129840f07ba76d7ba7fad5e6c6e54c5a9dc2af12b1f53d4247be1b93b250f7`
- Agent: `DEP-MLB-AGENT-1.5`
- Kernel: `DEP-MLB-KERNEL-0.7-REASONING-GUARD`
- Protocol: `DEPURACION_MLB_V0_5_REASONING_GUARD`
- Runtime status while validating: `DISABLED`
- Real-money authority: `FALSE`

A supported alias means FULL_AGENT_EXECUTION. Each real sports execution requires a new clean-room invocation, a new exclusive Drive folder/report, `AI_AGENT_ORCHESTRATOR` ownership, fresh same-run research, mandatory T100 consultation, F0→F10 and complete delivery.

## Sovereign sports burden

`@DepuracionMLB` is a Full Game Under pregame filter/orchestrator, not a betting authority. Every eligible game completes F2–F9 before final classification:

- F2 starter screen;
- F3 offense screen;
- F4 bullpen/context screen;
- F5 structural synthesis;
- F6 mandatory deep verification;
- F7 Full Game temporal viability;
- F8 materialization and symmetric countercase;
- F9 individual Red Team, final freshness, horizontal comparison and final classification.

Maximum final shortlist: two `PRIMARY_CANDIDATE`. `POTENTIAL_CANDIDATE` is disabled. Zero, one or two candidates are valid. The system never fills quota.

## V0.7 reasoning custody

Kernel 0.7 closes semantic gaps found while auditing V0.6. The AI remains the sports reasoner; the Kernel does not add scores or decide baseball by arithmetic.

### Claim genealogy

F2/F3/F4 claims are type-bound to their exact evidence family. F5–F9 synthesis claims require active `parent_claim_ids` from the causal predecessor phases. A terminal conclusion cannot be created directly from unrelated evidence.

Expected lineage:

`F2 STARTER_FINDING + F3 OFFENSE_FINDING + F4 BULLPEN_FINDING → F5 STRUCTURAL_SYNTHESIS → F6 DEEP_VERIFICATION_FINDING → F7 FULL_GAME_VIABILITY → F8 FAVORABLE_MECHANISM + ADVERSE_ROUTE → F9 FULL_GAME_SYNTHESIS → F9 FINAL_CLASSIFICATION`.

### F6 anti-hindsight precommit

Before the Deep Verification Web call, each game must freeze:

- discriminant question;
- prior hypothesis;
- falsifier;
- what would strengthen the architecture;
- what would degrade it;
- why the answer is material.

A DEEP_VERIFICATION event captured before this precommit is invalid.

### Independent mechanisms

Two labels do not count as two mechanisms merely because they are worded differently. A mechanism must contain an ID, family, side, `independence_key`, supporting claim IDs, causal path and non-overlap reason. A PRIMARY admission requires at least two distinct independence keys and non-identical supporting claim sets.

### Unknowns and conflicts

`UNKNOWN` and unresolved `CONFLICT` are structurally `NEUTRAL`. They cannot be assigned favorable or adverse direction. `UNKNOWN != RISK` and `UNKNOWN != SUPPORT` are enforced data rules, not only prose.

### F9 without ranking anchoring

F9 executes in three separated steps:

1. game-level pre-review without rank or final classification;
2. exact horizontal ordering of the full eligible universe;
3. final per-game classification reconciled to the pre-review and horizontal order.

Individual admission and comparative position are separate. A game may have `individual_gate=PASS`, survive Red Team and still finish `NOT_ADVANCED` because it ranks third: `OUTCOMPETED_COMPARATIVELY`. Kernel 0.7 therefore does not fabricate a false individual FAIL simply to explain a horizontal exclusion.

The horizontal order must contain every eligible game exactly once. `PRIMARY_CANDIDATE` is allowed only for the first two individually admissible games in that order.

## Final freshness

Before F9 pre-review, each game requires a same-run `FINAL_FRESHNESS_CHECK` that reconciles current:

- starter identity;
- lineup status;
- material absences;
- bullpen availability.

Old F1/F4 truth cannot silently survive into terminal ranking when a dynamic object changed.

## T100

Notion T100 is mandatory. Each run physically reads the canonical page, consultation protocol and the full `Vigente + Obligatorio` catalog snapshot. Cards are required by phase/game.

T100 is corrective memory, not a voting system. It may open questions, falsifiers and controls but cannot add points or replace current evidence.

Kernel 0.7 applies an explicit authority firewall. The following rule IDs are consulted but cannot govern Depuration ranking/classification because their operative scope belongs to market, F11/F12, certification or economic execution: `T100-17`, `T100-29`, `T100-35`, `T100-38`, `T100-40`, `T100-42`, `T100-44`, `T100-45`, `T100-GOB-01`, `T100-GOB-02`.

`T100-43` is also authority-boundary for this agent. Its rapid-mode option conflicts with the higher V0.5 constitution, which requires complete F2–F9 on all eligible games. F9 consults the relevant pregame F7/transversal rule set rather than blindly reusing postgame-oriented F8 mapping.

## Evidence and traceability

A completed game packet requires at least five sports evidence families under V0.7: `STARTER_PROFILE`, `OFFENSE_PROFILE`, `BULLPEN_CONTEXT`, `DEEP_VERIFICATION`, `FINAL_FRESHNESS`; at least two source origins; exact source ledger lineage; zero orphan Web events; zero unsourced/unreferenced active claims; F6 precommit; F9 pre-review; final ranking; verified Drive readback and `DATABASE_CONTROL_V07` PASS.

The minimum remains two independent source origins. Kernel 0.7 removed the non-sovereign three-origin/three-domain requirement that could create false negatives through bureaucracy.

## Market firewall

The agent does not open line, price, juice, sportsbook, EV, stake or units. V0.7 Web events must declare `market_inputs_used=false`; explicitly market-oriented research operations are rejected.

## Trust boundary

Tool-event hashes, snapshots and Drive readbacks are an operational trace physically observed by the connected runtime. They are not a provider-signed cryptographic attestation. V0.7 must state:

- `provider_cryptographic_attestation=false`
- `external_connector_trust_boundary=OPERATIONAL_TRACE_NOT_PROVIDER_SIGNED`

## Delivery

F10 requires 100% comparative closure, one frozen audited packet per eligible game, verified Source Ledger report, verified T100 trace, verified RUN_DOSSIER, verified HANDOFF_REPORT, frozen convergent `MLB_CHAT_REPORT_STANDARD_R1`, no unresolved own material blocker and `DELIVERY_STATUS=COMPLETE` before the earliest first pitch.

## Validation state

The prior controlled V0.6 run `TEST-DEP-AUTONOMOUS-E2E-V06-T100` was reclassified `AUDIT_ONLY` with `sports_certification_valid=false` after the V0.7 semantic audit found claim-family, genealogy, mechanism-independence, F6 precommit, F9 ranking and T100-scope defects.

Agent 1.5 / Kernel 0.7 remains `DISABLED` until a new controlled clean-room F0→F10 run, adversarial suite, terminal mutation tests and cross-system authority readback all pass. No real MLB sports conclusion is certified by infrastructure validation.

Vercel remains optional and outside connected-agent certification unless separately deployed and verified.
