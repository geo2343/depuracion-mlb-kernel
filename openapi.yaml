openapi: 3.1.0
info:
  title: DepuracionMLB Kernel
  version: 0.2.0
  description: Connected @DepuracionMLB control-plane metadata. Standalone sports execution remains fail-closed until a real MLB bridge and Vercel deployment are verified.
servers:
  - url: https://REPLACE-WITH-VERIFIED-VERCEL-DOMAIN
paths:
  /api/health:
    get:
      operationId: kernelHealth
      responses:
        '200':
          description: Truthful connected-agent health metadata
  /api/kernel:
    get:
      operationId: executeKernelTask
      responses:
        '503':
          description: Standalone MLB bridge not ready; no evidence is fabricated
